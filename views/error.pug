extends layout.pug 

block content 

    h1= `Server Error`
    h2= `Error status code: ${err.status} - Server Error`
    p= `${err.message}. Please go back.`

    nav.nav
        a(href='/')
            span &larr;
            |  Home