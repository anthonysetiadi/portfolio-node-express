extends layout.pug 

block content 

    h1= `${error.message} - Status: ${error.status}`
    p An error has occured. Please go back.

    nav.nav
        a(href='/')
            span &larr;
            |  Home